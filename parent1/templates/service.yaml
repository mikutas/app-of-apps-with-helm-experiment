apiVersion: v1
kind: Service
metadata:
  name: {{ include "parent1.fullname" . }}
  labels:
    {{- include "parent1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "parent1.selectorLabels" . | nindent 4 }}
