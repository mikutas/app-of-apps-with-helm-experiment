apiVersion: v1
kind: Service
metadata:
  name: {{ include "parent2.fullname" . }}
  labels:
    {{- include "parent2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "parent2.selectorLabels" . | nindent 4 }}
